{
  "name": "test-plugin",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:components": "node scripts/build-components.js",
    "build": "npm run build --workspaces"
  }
}
